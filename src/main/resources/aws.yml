## AWS S3 설정

cloud:
  aws:
    credentials:
      accessKey: ${accessKey}
      secretKey: ${secretKey}
    s3:
      bucket: superkurly-bucket
    region:
      static: ap-northeast-2
    stack:
      auto: false
#      credentials:
#        instanceProfile: true

